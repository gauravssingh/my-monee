# Per-Mac settings. Copy this file to:
# ~/Library/Application Support/ExpenseTracker/config.local.yaml
#
# This file is intentionally safe to commit. Do not put OAuth JSON, tokens, or passwords here.

app: {}
  # data_dir: ~/Library/Application Support/ExpenseTracker
  # port: 8477

database: {}
  # filename: expense_tracker.db
  # echo: false

gmail: {}
  # Keep OAuth client JSON outside the repository. By default it is stored in data_dir.
  # credentials_file: ~/Library/Application Support/ExpenseTracker/gmail_credentials.json
  # sync_after_date: "2026/01/01"

scheduler: {}
  # enabled: true
  # interval_minutes: 15
